<h1 class="text-center"><%= @topic.title %></h1>
<div class="container">
  <div class="row">
    <ul class="col-md-3">
        <% @bookmarks.each do |bookmark| %>
          <li><%= link_to bookmark.url, bookmark.url %></p>
        <% end %>
      </p>
    </div>
  </div>
</div>
